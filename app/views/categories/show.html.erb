<h1>Category Show</h1>

<% @items.each do |item| %>
  <%= item.title %><br>
  <%= item.price %><br>
  <%= item.description %><br>
  <%= button_to "Add to Cart", cart_path(item_id: item.id)  , method: :get %>
<% end %>
